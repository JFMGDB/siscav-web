# URL base da API REST do backend siscav-api.
# Em desenvolvimento local costuma ser http://localhost:8000 (igual ao fallback em src/constants/index.ts quando NEXT_PUBLIC_API_URL não está definida).
NEXT_PUBLIC_API_URL=http://localhost:8000
