---
phase: 04-monitor-ux-capture-truthfulness
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/hooks/use-monitor-capture.ts
  - src/components/features/monitor/PlateRecognitionDisplay.tsx
autonomous: true
requirements:
  - MON-01
  - MON-02
must_haves:
  truths:
    - "PlateRecognitionDisplay shows distinct loading, error, empty, and success states per 04-UI-SPEC.md."
    - "Confidence UI block renders only when capture.confidence is defined."
  artifacts:
    - path: "src/components/features/monitor/PlateRecognitionDisplay.tsx"
      provides: "Alert + retry + loading/empty copy + conditional confidence"
      contains: "isError|refetch|Sem leituras"
    - path: "src/hooks/use-monitor-capture.ts"
      provides: "Query flags for UI branches"
      contains: "isError|isFetching"
  key_links:
    - from: "src/components/features/monitor/PlateRecognitionDisplay.tsx"
      to: ".planning/phases/04-monitor-ux-capture-truthfulness/04-UI-SPEC.md"
      via: "copy and state order"
      pattern: "UI-SPEC"
---

<objective>
Implement **monitor last-capture UX** per `04-UI-SPEC.md`: Query error vs. loading vs. empty vs. success, and conditional confidence (MON-01 + MON-02 UI).

Purpose: Operators see errors as errors, empty as empty, and no invented confidence.

Output: Updated hook surface and `PlateRecognitionDisplay` only (not `CameraFeed` / `ManualRegistrationForm`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-monitor-ux-capture-truthfulness/04-CONTEXT.md
@.planning/phases/04-monitor-ux-capture-truthfulness/04-UI-SPEC.md
@.planning/phases/04-monitor-ux-capture-truthfulness/04-01-PLAN.md

<interfaces>
`useMonitorCapture` returns `capture`, `loading`, `error`, `refetch` — add **`isError`** and **`isFetching`** for explicit branches.

**04-UI-SPEC** locks: error title/body/CTA, loading lines, empty lines, success card title/subtitle, confidence conditional, typography weights **400** and **600** only, placa `h2` `fontWeight: 600` if needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose isError and isFetching from useMonitorCapture</name>
  <files>src/hooks/use-monitor-capture.ts</files>
  <read_first>
    - `src/hooks/use-monitor-capture.ts`
    - `@tanstack/react-query` useQuery return shape in this project version
  </read_first>
  <acceptance_criteria>
    - Return object includes `isError: boolean` (from `query.isError` or equivalent).
    - Return object includes `isFetching: boolean` for optional discrete revalidation UI.
    - Existing consumers unchanged except `PlateRecognitionDisplay` in Task 2.
  </acceptance_criteria>
  <action>
    Pass through `query.isError`, `query.isFetching`. Keep `loading` mapped from `query.isLoading` (or project-consistent pending flag).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>UI can branch on error without inferring from error object alone.</done>
</task>

<task type="auto">
  <name>Task 2: PlateRecognitionDisplay state tree + confidence conditional</name>
  <files>src/components/features/monitor/PlateRecognitionDisplay.tsx</files>
  <read_first>
    - `.planning/phases/04-monitor-ux-capture-truthfulness/04-UI-SPEC.md` — Estados, Copywriting, Typography, Color
    - `src/components/features/monitor/PlateRecognitionDisplay.tsx` — full file
  </read_first>
  <acceptance_criteria>
    - Render order: **isError** first → **loading** (initial) → **success + !capture** empty → **success + capture** detail.
    - Error: MUI `Alert` `severity="error"` with UI-SPEC title/body + `Button` “Tentar novamente” calling `refetch`; **no** spinner as sole error affordance.
    - Loading: `CircularProgress` + exact loading title/subtitle from UI-SPEC (PT).
    - Empty: exact empty title/body from UI-SPEC; **not** the string “Aguardando veículo...”.
    - Success: existing plate/status layout; **confidence** row only if `capture.confidence != null && capture.confidence !== undefined` (use explicit check suitable for `0` if API ever sends 0).
    - Placa `Typography` `variant="h2"` uses `sx={{ fontWeight: 600 }}` if default theme weight would exceed the two-weight contract.
    - `rg "as any" src/components/features/monitor/PlateRecognitionDisplay.tsx` — no matches.
  </acceptance_criteria>
  <action>
    Import `Alert`, `Button` as needed. Wire copy verbatim from UI-SPEC Copywriting table.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Monitor card matches UI contract and MON requirements.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests</name>
  <files>package.json</files>
  <read_first>
    - `package.json` — scripts.test
  </read_first>
  <acceptance_criteria>
    - `npm test` exit 0.
  </acceptance_criteria>
  <action>
    Run full Jest suite; fix only failures caused by this phase.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Suite green.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npm test` from repository root.
- **REQUIREMENTS trace:** Set MON-01 and MON-02 to `[x]` and trace **Done**; add `04-VERIFICATION.md` **passed** when phase complete.
</verification>

<success_criteria>
- UI-SPEC state order and copy satisfied.
- No confidence display without API-backed value.
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitor-ux-capture-truthfulness/04-02-SUMMARY.md`
</output>
