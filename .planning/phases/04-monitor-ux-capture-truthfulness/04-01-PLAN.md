---
phase: 04-monitor-ux-capture-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/monitor.ts
  - src/lib/api/monitor.ts
autonomous: true
requirements:
  - MON-01
  - MON-02
must_haves:
  truths:
    - "getLastCapture returns null only when getLogs succeeds with zero items; failures propagate as thrown errors."
    - "Capture objects do not include a fabricated confidence value; confidence is optional on the type."
  artifacts:
    - path: "src/lib/api/monitor.ts"
      provides: "getLastCapture without error-swallowing catch"
      contains: "getLastCapture"
    - path: "src/types/monitor.ts"
      provides: "Capture.confidence optional"
      contains: "confidence"
  key_links:
    - from: "src/lib/api/monitor.ts"
      to: "src/lib/api/logs.ts"
      via: "getLogs"
      pattern: "getLogs"
---

<objective>
Align the **data layer** for last capture with MON-01 (errors visible to React Query) and MON-02 (no fake confidence). UI changes are plan **04-02**.

Purpose: `queryFn` can distinguish failure vs. empty success.

Output: Updated `Capture` type and `getLastCapture` implementation only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-monitor-ux-capture-truthfulness/04-CONTEXT.md
@.planning/phases/04-monitor-ux-capture-truthfulness/04-UI-SPEC.md
@.planning/codebase/CONCERNS.md

<interfaces>
`getLastCapture` today wraps `getLogs` in `try/catch` and returns `null` on any error; sets `confidence: 0.95` always.

`Capture` in `src/types/monitor.ts` has required `confidence: number`.

Do **not** add `confidence` to `AccessLog` unless the executor confirms the API field exists (out of scope by default).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture type + getLastCapture truthful mapping</name>
  <files>src/types/monitor.ts
src/lib/api/monitor.ts</files>
  <read_first>
    - `src/types/monitor.ts`
    - `src/lib/api/monitor.ts`
    - `src/lib/api/logs.ts` — `getLogs`
  </read_first>
  <acceptance_criteria>
    - `confidence?: number` on `Capture` (optional).
    - No `try/catch` that converts arbitrary failures into `return null` for `getLastCapture`.
    - On successful `getLogs` with `items.length === 0`, return `null`.
    - On success with a row, build `Capture` **without** `confidence: 0.95` (omit `confidence` unless mapping from a real API field later).
    - `rg "0\\.95" src/lib/api/monitor.ts` — **no** matches.
  </acceptance_criteria>
  <action>
    Edit both files in one logical change. Let `getLogs` rejections propagate.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Data path supports Query error vs. null success; no fake confidence.</done>
</task>

</tasks>

<verification>
- From repository root: `npm run build` and `npm test` after Task 1.
- **Trace:** Prefer updating `REQUIREMENTS.md` MON-01/MON-02 together with plan **04-02** completion and `04-VERIFICATION.md`.
</verification>

<success_criteria>
- Failures from `getLogs` / network are not masked as `null`.
- No hardcoded confidence in `monitor.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitor-ux-capture-truthfulness/04-01-SUMMARY.md`
</output>
