---
phase: 03-type-safety-ui-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/DataTable.tsx
  - src/components/features/whitelist/WhitelistTable.tsx
  - src/components/features/logs/LogsTable.tsx
autonomous: true
requirements:
  - TYP-01
must_haves:
  truths:
    - "DataTable column definitions do not use `any` for format or cell value reads where domain keys apply."
    - "Synthetic action columns (e.g. Whitelist) are typed without `(row as any)`."
  artifacts:
    - path: "src/components/ui/DataTable.tsx"
      provides: "Typed Column union / keyof T cell access"
      contains: "Column"
    - path: "src/components/features/whitelist/WhitelistTable.tsx"
      provides: "Columns compatible with new Column API"
      contains: "columns:"
  key_links:
    - from: "src/components/features/logs/LogsTable.tsx"
      to: "src/components/ui/DataTable.tsx"
      via: "Column<AccessLog>"
      pattern: "DataTable"
---

<objective>
Remove `any` from `DataTable` column `format` and from row field reads (TYP-01, CONTEXT D-01–D-02). Adjust `WhitelistTable` and `LogsTable` column defs to the new API, including a **safe pattern** for the non-domain `actions` column (CONTEXT: exception minimal, prefer consumer fix).

Purpose: Tables stay generic and domain-typed; no visual or behavioral change.

Output: Updated `DataTable.tsx` exports; both consumers compile with stricter types.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-type-safety-ui-data/03-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
`DataTable.tsx` today: `Column<T>` with `format?: (value: any, row: T) => React.ReactNode` and `(row as any)[column.id]`.

`WhitelistTable.tsx`: columns include `id: 'actions'` (not a key of `AuthorizedPlate`).

`LogsTable.tsx`: column ids `timestamp`, `plate_string_detected`, `status`, `image_storage_key` — all keys of `AccessLog`. **Do not** change the Status `Select` / `as any` here (TYP-02 / plan 03-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Column types and cell rendering in DataTable</name>
  <files>src/components/ui/DataTable.tsx</files>
  <read_first>
    - `src/components/ui/DataTable.tsx` — full file.
    - `src/types/logs.ts`, `src/types/whitelist.ts` — shape sanity (no edits required unless a shared helper is needed).
  </read_first>
  <acceptance_criteria>
    - `rg "\\bany\\b" src/components/ui/DataTable.tsx` — **no** `any` in `Column` / `DataTableProps` public types or in the cell value path (internal `React` namespace ok).
    - Data columns use `keyof T` (or equivalent) so `row[column.id]` is typed without assertion to `any`.
    - Synthetic columns (e.g. `actions`) supported via a **documented** pattern: discriminated union, literal `'actions'`, or separate `ActionColumn<T>` — no `(row as any)`.
  </acceptance_criteria>
  <action>
    Replace `any` in `format` and row indexing. Implement the CONTEXT-approved approach: prefer `keyof T` for data columns plus an explicit variant for non-field columns (e.g. `type Column<T> = DataColumn<T> | ActionColumn<T>` where action column `format` receives `row` only or `value` is `never`). Keep `T extends { id: string | number }` for row keys.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>DataTable exports strict column types and renders cells without `as any`.</done>
</task>

<task type="auto">
  <name>Task 2: Align WhitelistTable and LogsTable column defs</name>
  <files>src/components/features/whitelist/WhitelistTable.tsx
src/components/features/logs/LogsTable.tsx</files>
  <read_first>
    - `src/components/ui/DataTable.tsx` — new `Column` / exports.
    - Both table files — full column arrays.
  </read_first>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - Whitelist `actions` column uses the new API (typed).
    - LogsTable column `format` callbacks typecheck against `AccessLog` fields; **still** may contain `as any` on Select only until plan 03-02 (grep `LogsTable.tsx` for `as any` — acceptable if **only** inside Select `onChange`).
  </acceptance_criteria>
  <action>
    Update `columns` in both files to satisfy the new `Column<T>` types. Fix `format` parameter types and any `id` literals to match `AuthorizedPlate` / `AccessLog` keys or the action-column variant.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Both tables compile; DataTable integration matches TYP-01.</done>
</task>

</tasks>

<verification>
- From repository root: `npm run build` and `npm test` after both tasks.
- **Executor — REQUIREMENTS trace:** When this plan is complete, leave **TYP-01** checkbox and trace row updates for when **all** phase 3 plans finish (or update TYP-01 only if you execute this plan in isolation with verification — prefer batch trace update after 03-03 per project convention). *Default:* update `REQUIREMENTS.md` TYP-01 to `[x]` and trace **Done** only after full phase 3 verification (03-VERIFICATION.md).
</verification>

<success_criteria>
- No `any` in public DataTable column typing or cell read path.
- Whitelist and Logs tables compile with the new column model.
</success_criteria>

<output>
After completion, create `.planning/phases/03-type-safety-ui-data/03-01-SUMMARY.md`
</output>
