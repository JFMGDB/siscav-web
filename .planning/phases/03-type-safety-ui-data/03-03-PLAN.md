---
phase: 03-type-safety-ui-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/devices.ts
  - src/types/index.ts
  - src/lib/api/devices.ts
  - src/lib/api-client.ts
autonomous: true
requirements:
  - TYP-03
must_haves:
  truths:
    - "devices API module does not expose Promise<unknown> / Promise<unknown[]> for scan, connect, or disconnect in its public signatures."
    - "Types live under src/types/ and are exported for consumers."
  artifacts:
    - path: "src/lib/api/devices.ts"
      provides: "Typed return signatures"
      contains: "scanDevices|connectDevice|disconnectDevice"
    - path: "src/types/devices.ts"
      provides: "DTOs or aliases for device API"
      contains: "export"
  key_links:
    - from: "src/lib/api-client.ts"
      to: "src/lib/api/devices.ts"
      via: "scanDevices|connectDevice|disconnectDevice"
      pattern: "devices"
---

<objective>
Replace `unknown` / `unknown[]` in `src/lib/api/devices.ts` (and matching facade methods in `api-client.ts`) with concrete types from `src/types/` (TYP-03, CONTEXT D-04–D-05). Align names with actual JSON where known; document any intentional difference from Web Bluetooth `BluetoothDevice` in `src/types/bluetooth.ts`.

Purpose: Call sites and tests get compile-time guarantees without changing HTTP behavior.

Output: New or extended types file, updated API module and facade signatures, green build/tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-type-safety-ui-data/03-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
`devices.ts`: `scanDevices` → `unknown[]`; `connectDevice` / `disconnectDevice` → `unknown`. `getConnectionStatus` already `ConnectionStatus`. `sendVideoFrame` → `void`.

`BluetoothDevice` in `src/types/bluetooth.ts` has `id`, `name`, `address`, optional `rssi`, `paired`, `connected`. REST payloads may omit or rename fields — use dedicated DTOs if needed.

`api-client.ts` mirrors return types for `scanDevices`, `connectDevice`, `disconnectDevice`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add device DTO types and wire devices.ts + facade</name>
  <files>src/types/devices.ts
src/types/index.ts
src/lib/api/devices.ts
src/lib/api-client.ts</files>
  <read_first>
    - `src/lib/api/devices.ts` — full file.
    - `src/lib/api-client.ts` — device methods.
    - `src/types/bluetooth.ts` — reuse vs new types.
    - `src/components/features/settings/BluetoothDeviceManager.tsx` — how backend responses are consumed (if at all).
  </read_first>
  <acceptance_criteria>
    - New file `src/types/devices.ts` (or explicit decision to extend `bluetooth.ts` only — CONTEXT allows either) exporting types used by the API layer.
    - `scanDevices` return type is a **named** array element type (e.g. `DeviceScanItem[]` or `BluetoothDevice[]` with a one-line comment if shapes match).
    - `connectDevice` and `disconnectDevice` use concrete return types; `Promise<void>` acceptable if responses are unused and API returns empty JSON.
    - `rg "Promise<unknown" src/lib/api/devices.ts` — **no** matches (exported helpers must not return `Promise<unknown>` / `Promise<unknown[]>`).
    - `src/types/index.ts` re-exports new types if in `devices.ts`.
    - Facade `api-client.ts` signatures match `devices.ts`.
  </acceptance_criteria>
  <action>
    Define minimal DTOs sufficient for typing. Use `client.request<T>` with those T. Do not change endpoints or headers. If backend shape is uncertain, prefer a narrow interface (e.g. `{ id: string }`) plus comment to widen when contract is confirmed.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>API layer typed end-to-end for scan/connect/disconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests</name>
  <files>package.json</files>
  <read_first>
    - `package.json` — `scripts.test`; no new test files required unless failures appear.
  </read_first>
  <acceptance_criteria>
    - `npm test` exits 0.
  </acceptance_criteria>
  <action>
    Run full Jest suite; fix only failures caused by this plan’s changes.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Tests green after TYP-03 typing.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npm test` from repository root.
- **REQUIREMENTS trace:** When phase 3 is fully verified, set TYP-03 `[x]` and trace **Done** (coordinate with 03-01/03-02 trace updates — all three TYP-* should flip together on phase completion).
</verification>

<success_criteria>
- No `unknown` returns for scan/connect/disconnect in public API signatures.
- Types exported from `src/types/` and used consistently in `api-client.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-type-safety-ui-data/03-03-SUMMARY.md`
</output>
