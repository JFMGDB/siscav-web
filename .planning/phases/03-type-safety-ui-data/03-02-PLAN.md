---
phase: 03-type-safety-ui-data
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/features/logs/LogsTable.tsx
autonomous: true
requirements:
  - TYP-02
must_haves:
  truths:
    - "LogsTable status filter Select onChange is typed; no `as any` cast for that handler."
  artifacts:
    - path: "src/components/features/logs/LogsTable.tsx"
      provides: "SelectChangeEvent or equivalent narrowing"
      contains: "setStatusFilter"
  key_links:
    - from: "src/components/features/logs/LogsTable.tsx"
      to: "@mui/material"
      via: "SelectChangeEvent"
      pattern: "SelectChangeEvent|statusFilter"
---

<objective>
Remove `as any` from the MUI `Select` `onChange` that drives `statusFilter` in `LogsTable` (TYP-02, CONTEXT D-03). No UI copy or layout changes.

Purpose: Typecheck covers filter values aligned with `MenuItem` options and state.

Output: Typed handler; `rg "as any" src/components/features/logs/LogsTable.tsx` returns no matches **or** only matches outside the Select onChange (should be zero for this file if possible).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-type-safety-ui-data/03-CONTEXT.md
@.planning/phases/03-type-safety-ui-data/03-01-PLAN.md

<interfaces>
`statusFilter` state: `'ALL' | 'Authorized' | 'Denied'`. `MenuItem` values match. Current line uses `setStatusFilter(e.target.value as any)`.

Depends on plan 03-01 so `Column<AccessLog>` and `DataTable` typings already compile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type Select onChange for status filter</name>
  <files>src/components/features/logs/LogsTable.tsx</files>
  <read_first>
    - `src/components/features/logs/LogsTable.tsx` — filters block and imports.
    - MUI types: `SelectChangeEvent` from `@mui/material/Select` or `@mui/material`.
  </read_first>
  <acceptance_criteria>
    - Define a type alias e.g. `type LogStatusFilter = 'ALL' | 'Authorized' | 'Denied'` (or reuse literals).
    - Handler uses `SelectChangeEvent<LogStatusFilter>` (or equivalent) so `event.target.value` is typed without `as any`.
    - `rg "as any" src/components/features/logs/LogsTable.tsx` — **no** matches.
    - `filters` / `useMemo` still passes `AccessLogFilters` correctly (`status` when not `ALL`).
  </acceptance_criteria>
  <action>
    Replace the cast with MUI’s typed change event. If needed, narrow with a small helper `isLogStatusFilter(value: string): value is LogStatusFilter` for exhaustive safety.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Status filter typing satisfies TYP-02.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npm test` from repository root.
- **Trace:** Prefer updating TYP-02 in `REQUIREMENTS.md` together with final phase verification; if executing alone, mark TYP-02 Done when phase 3 complete.
</verification>

<success_criteria>
- No `as any` in `LogsTable.tsx` for the Select handler.
- Build and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-type-safety-ui-data/03-02-SUMMARY.md`
</output>
