---
phase: 02-api-client-http-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api/client.ts
  - src/lib/api-client.ts
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - "Browser reads of access_token and refresh_token use the same encode/decode rules as ApiClient cookie persistence."
    - "apiClient.getAccessToken and getRefreshToken do not parse document.cookie with split/startsWith on raw names."
    - "setAccessToken/setRefreshToken remain documented no-ops (cookies set via login/setTokens only)."
  artifacts:
    - path: "src/lib/api/client.ts"
      provides: "Exported cookie readers + singleton using them"
      contains: "export function read"
    - path: "src/lib/api-client.ts"
      provides: "Facade token getters delegate to client helpers"
      contains: "readBrowser"
  key_links:
    - from: "src/lib/api-client.ts"
      to: "src/lib/api/client.ts"
      via: "imported read helpers"
      pattern: "from '@/lib/api/client'"
---

<objective>
Unify browser cookie **read** paths for auth tokens so `getClientApiClient()` and the `apiClient` facade share one implementation with URL-encoding aligned to existing `setCookie`/`getCookie` in `client.ts` (per D-01, D-02, D-03).

Purpose: Eliminates divergent token reads (encoded cookies misread by the facade).

Output: Exported read helpers on `client.ts`, refactored singleton, facade delegating only.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-client-http-consistency/02-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
From `src/lib/api/client.ts` (current — preserve `parseApiError`, `ApiClient`, exports after refactor):

- Private `getCookie(name)` uses `encodeURIComponent(name)` in the regex and `decodeURIComponent(match[1])`.
- `getClientSingleton()` wires `getToken: () => getCookie(AUTH_CONFIG.ACCESS_TOKEN_KEY)` and `getRefreshToken: () => getCookie(AUTH_CONFIG.REFRESH_TOKEN_KEY)`.
- `AUTH_CONFIG.ACCESS_TOKEN_KEY` / `REFRESH_TOKEN_KEY` from `@/constants`.

From `src/lib/api-client.ts`:

- `getAccessToken` / `getRefreshToken` today use `document.cookie.split(';')` + `startsWith('access_token=')` — **remove** (D-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export cookie readers and wire singleton (D-01, D-02)</name>
  <files>src/lib/api/client.ts</files>
  <read_first>
    - `src/lib/api/client.ts` — full file: `getCookie`, `getClientSingleton`, `AUTH_CONFIG` usage.
    - `src/constants/index.ts` — `AUTH_CONFIG.ACCESS_TOKEN_KEY`, `REFRESH_TOKEN_KEY`.
  </read_first>
  <acceptance_criteria>
    - `rg "function getCookie" src/lib/api/client.ts` — either `getCookie` stays private and new exports wrap it, or it is exported; encoding must match existing regex + `decodeURIComponent`.
    - `rg "getToken:" src/lib/api/client.ts` — `getToken` and `getRefreshToken` in `getClientSingleton` call the **same** exported helpers (not duplicate inline regex).
    - `rg "export function read" src/lib/api/client.ts` — at least one new export for browser token reads (exact name per discretion: e.g. `readBrowserAccessToken`, `readBrowserRefreshToken`, or `readAuthCookieFromDocument`).
  </acceptance_criteria>
  <action>
    Extract the existing `getCookie` logic into the single source of truth. Export minimal helpers per D-02 (names at executor discretion) that read `AUTH_CONFIG.ACCESS_TOKEN_KEY` and `AUTH_CONFIG.REFRESH_TOKEN_KEY` using the **same** encode/decode behavior as the current private `getCookie`. Refactor `getClientSingleton()` so `getToken` and `getRefreshToken` options call those exported helpers only — no duplicated `document.cookie` parsing paths inside the singleton factory.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Singleton and exported helpers share one cookie-read implementation; behavior matches prior `getCookie` semantics for encoded names/values.</done>
</task>

<task type="auto">
  <name>Task 2: Facade delegates token getters; no-op setters documented (D-01, D-03)</name>
  <files>src/lib/api-client.ts</files>
  <read_first>
    - `src/lib/api-client.ts` — `getAccessToken`, `getRefreshToken`, `setAccessToken`, `setRefreshToken`.
    - `src/lib/api/client.ts` — newly exported read helpers from Task 1.
  </read_first>
  <acceptance_criteria>
    - `rg "split\\(';'\\)" src/lib/api-client.ts` — **no** matches inside `getAccessToken` / `getRefreshToken` (entire facade may still use `;` elsewhere — zero cookie-splitting for token keys).
    - `rg "startsWith\\('access_token" src/lib/api-client.ts` — no matches.
    - `rg "getAccessToken|getRefreshToken" src/lib/api-client.ts` — bodies import and call only the shared helpers from `@/lib/api/client`.
    - `setAccessToken` / `setRefreshToken` remain no-ops with a one-line comment that cookies are set via `login` / `ApiClient.setTokens` (D-03).
  </acceptance_criteria>
  <action>
    Replace manual `document.cookie` parsing in `apiClient.getAccessToken` and `getRefreshToken` with delegation to the exported readers from Task 1. Preserve public signatures (`(): string | null`). Keep server/SSR safety: return `null` when `document` is undefined (same as today’s guard pattern). Do not add cookie writes to setters.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Facade reads tokens identically to `ApiClient` browser cookie reads; no duplicate parse path for access/refresh keys.</done>
</task>

</tasks>

<verification>
- From repository root: run `npm run build` and `npm test` after both tasks (D-07).
- **Executor — REQUIREMENTS trace:** When this plan is complete, in `.planning/REQUIREMENTS.md` set **API-01** checkbox to `[x]` and set the trace table row for API-01 **Status** to `Done`.
</verification>

<success_criteria>
- Cookie read path is unified: singleton and `apiClient.getAccessToken` / `getRefreshToken` use exported helpers from `client.ts`.
- No raw `access_token=` / `refresh_token=` string splitting in `api-client.ts` for token reads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-client-http-consistency/02-01-SUMMARY.md`
</output>
