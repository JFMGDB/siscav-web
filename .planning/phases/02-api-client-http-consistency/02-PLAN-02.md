---
phase: 02-api-client-http-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/api/client.ts
  - src/lib/api/auth.ts
autonomous: true
requirements:
  - API-02
must_haves:
  truths:
    - "register performs an unauthenticated JSON POST to the API base URL derived from the ApiClient instance (no Authorization header, no ApiClient.request refresh loop)."
    - "Error bodies for register are parsed with parseApiError like other client paths; maintenance comment documents the exception to ApiClient.request."
    - "Base URL for register cannot drift from the trimmed ApiClient baseUrl (getBaseUrl or documented equivalence)."
  artifacts:
    - path: "src/lib/api/client.ts"
      provides: "ApiClient.getBaseUrl()"
      contains: "getBaseUrl"
    - path: "src/lib/api/auth.ts"
      provides: "register using fetch + parseApiError + base from client"
      contains: "register("
  key_links:
    - from: "src/lib/api/auth.ts"
      to: "src/lib/api/client.ts"
      via: "parseApiError + client.getBaseUrl()"
      pattern: "parseApiError|getBaseUrl"
---

<objective>
Align `register` with the shared HTTP/error story: no bearer on the wire, no refresh-on-401 via `ApiClient.request`, same `parseApiError` usage, and base URL tied to the `ApiClient` instance via `getBaseUrl()` (D-04, D-05, D-06).

Purpose: One documented auth HTTP pattern; avoids attaching an existing session token to signup.

Output: `getBaseUrl()` on `ApiClient`, refactored `register()`, English maintenance block above `register`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-api-client-http-consistency/02-CONTEXT.md
@.planning/phases/02-api-client-http-consistency/02-PLAN-01.md

<interfaces>
After Plan 01, `src/lib/api/client.ts` exports browser cookie readers; `ApiClient` still has `request`, `setTokens`, `parseApiError` export.

`src/lib/api/auth.ts` — `register(_client, email, password)` currently uses `fetch(\`${API_CONFIG.BASE_URL}${API_CONFIG.ENDPOINTS.AUTH.REGISTER}\`, ...)` and `parseApiError`. Must use instance base URL per D-06.

`src/lib/api-client.ts` — `register` calls `authApi.register(client(), email, password)`; keep signature — pass real `ApiClient` so `getBaseUrl()` is available.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose getBaseUrl on ApiClient (D-06)</name>
  <files>src/lib/api/client.ts</files>
  <read_first>
    - `src/lib/api/client.ts` — `ApiClient` constructor `this.baseUrl` assignment (`replace(/\/$/, '')`).
  </read_first>
  <acceptance_criteria>
    - `rg "getBaseUrl" src/lib/api/client.ts` — public method `getBaseUrl(): string` returning the normalized base URL (no trailing slash, same as used in `fetch(\`${this.baseUrl}${endpoint}\`)`).
    - `rg "class ApiClient" -A 80 src/lib/api/client.ts` — no behavioral change to `request` / refresh logic in this task.
  </acceptance_criteria>
  <action>
    Add `getBaseUrl(): string` to `ApiClient` that returns `this.baseUrl` (the already-trimmed value from the constructor). This is the single read-only accessor for the instance base URL used by `register` and future call sites that must avoid `API_CONFIG.BASE_URL` drift.
  </action>
  <verify>
    <automated>cd C:\src\personal\siscav-web && npm run build</automated>
  </verify>
  <done>Consumers can obtain the exact base URL string used by `ApiClient.request`.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor register — no auth header, parseApiError, base from client (D-04, D-05, D-06)</name>
  <files>src/lib/api/auth.ts</files>
  <read_first>
    - `src/lib/api/auth.ts` — full file: `register`, `login`, imports.
    - `src/lib/api/client.ts` — `parseApiError`, `API_CONFIG.ENDPOINTS` usage pattern if needed for endpoint path only.
  </read_first>
  <acceptance_criteria>
    - `rg "register\\(" src/lib/api/auth.ts` — first parameter is used (rename `_client` → `client` or equivalent); URL built as `` `${client.getBaseUrl()}${API_CONFIG.ENDPOINTS.AUTH.REGISTER}` `` (or equivalent using `getBaseUrl()` + register endpoint constant).
    - `rg "Authorization" src/lib/api/auth.ts` — **no** `Authorization` header in `register`’s `fetch` options.
    - `rg "\\.request\\(" src/lib/api/auth.ts` — **no** `client.request` call inside `register` (D-04: must not go through bearer + refresh path).
    - `rg "parseApiError" src/lib/api/auth.ts` — still used for non-OK responses in `register`.
    - English maintenance comment block **immediately above** `register` stating: (1) intentionally no bearer / not using `ApiClient.request`; (2) no refresh cycle on 401 for this call; (3) errors via `parseApiError` aligned with the rest of the client; (4) base URL via `getBaseUrl()` matches the `ApiClient` instance (equivalent to `API_CONFIG.BASE_URL` for the default singleton) — per D-05.
    - Preserve existing 409 / 429 special-case `throw new Error(...)` behavior after `parseApiError` where applicable.
  </acceptance_criteria>
  <action>
    Implement `register(client: ApiClient, ...)` using `fetch` with `method: 'POST'`, `Content-Type: application/json`, body `JSON.stringify({ email, password })`, **no** `Authorization` header. On `!res.ok`, keep using `parseApiError(res)` for messages; retain status-specific branches (409, 429) as today. Build URL with `client.getBaseUrl()` + `API_CONFIG.ENDPOINTS.AUTH.REGISTER`. Add the short English maintenance block per D-05 above the function.
  </action>
  <verify>
    <automated>cd C:\src\personal\siscav-web && npm test</automated>
  </verify>
  <done>Register uses instance base URL, shared error parsing, and is documented as the intentional non-`request` auth path.</done>
</task>

</tasks>

<verification>
- Run `npm run build` and `npm test` (D-07). Fix tests only if old expectations encoded wrong cookie/token behavior; otherwise update with justification in commit message.
- **Executor — REQUIREMENTS trace:** When this plan is complete, in `.planning/REQUIREMENTS.md` set **API-02** checkbox to `[x]` and set the trace table row for API-02 **Status** to `Done`.
</verification>

<success_criteria>
- `register` does not use `ApiClient.request` and does not send `Authorization`.
- `register` uses `parseApiError` and `client.getBaseUrl()`; maintenance comment explains why.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-client-http-consistency/02-02-SUMMARY.md`
</output>
