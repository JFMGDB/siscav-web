---
phase: 05-auth-hook-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-auth.tsx
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Bootstrap finishes loading only after sync paths and awaited refresh path complete; refresh-only cookie session does not flip isLoading false before refresh settles."
    - "react-hooks/exhaustive-deps is satisfied without disable, OR a single eslint-disable-next-line react-hooks/exhaustive-deps is preceded by a multi-line comment explaining mount-only intent and reviewer follow-up."
  artifacts:
    - path: "src/hooks/use-auth.tsx"
      provides: "Documented bootstrap; no blind exhaustive-deps suppression"
      contains: "AuthProvider"
  key_links:
    - from: "src/hooks/use-auth.tsx"
      to: "src/lib/api-client.ts"
      via: "getAccessToken|getRefreshToken|refreshTokens|clearTokens"
      pattern: "apiClient"
---

<objective>
Refactor **`AuthProvider` bootstrap** in `use-auth.tsx` to meet **AUTH-01** and **05-CONTEXT** D-01–D-04: remove the race where `setIsLoading(false)` runs while `refreshTokens()` is still in flight; prefer **no** `eslint-disable-next-line react-hooks/exhaustive-deps` without a documented reason, or a **minimal** disable with a **block comment** immediately above it (what the effect does, why deps are `[]`, when to revisit).

Purpose: Reviewers and eslint understand the hook; protected layout does not redirect to login during a valid refresh.

Output: Updated `src/hooks/use-auth.tsx` only in this plan.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-auth-hook-reliability/05-CONTEXT.md
@.planning/codebase/CONCERNS.md

<interfaces>
Current bootstrap (`useEffect` with `[]`): reads `apiClient.getAccessToken()`, `getRefreshToken()`, `localStorage` user; on `refreshToken && !accessToken` calls `apiClient.refreshTokens()` **without await**; then **always** `setIsLoading(false)`.

`apiClient.refreshTokens()` (`src/lib/api-client.ts` → `getClientApiClient().refreshTokens()`) updates cookies via `setTokensImpl` on success; it does **not** return user JSON — after refresh, **reuse `storedUser` from localStorage** if still present and valid JSON (same as access+user path).

`login` / `logout` / `register` stay behavior-compatible unless a bootstrap edge case requires a tiny shared helper (keep public API of context unchanged: `user`, `login`, `logout`, `isLoading`, `register`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async bootstrap with awaited refresh and documented deps</name>
  <files>src/hooks/use-auth.tsx</files>
  <read_first>
    - `src/hooks/use-auth.tsx` — full file
    - `src/lib/api-client.ts` — `getAccessToken`, `getRefreshToken`, `refreshTokens`, `clearTokens`
    - `src/lib/api/client.ts` — `refreshTokens` body (cookies updated on success)
    - `src/constants/index.ts` — `AUTH_CONFIG.USER_KEY` if needed
  </read_first>
  <acceptance_criteria>
    - On code path `refreshToken && !accessToken` (same logical condition as today), the bootstrap **awaits** `apiClient.refreshTokens()` (or equivalent single-flight await) **before** calling `setIsLoading(false)` for that execution path; use `try/catch` or `.catch` so failures still end loading after clearing tokens as today.
    - After successful refresh in that path, if `localStorage` still has `AUTH_CONFIG.USER_KEY` and `apiClient.getAccessToken()` is non-null, **setUser** from parsed JSON (same try/catch clear behavior as access+user branch on parse failure).
    - **Either:** `npx eslint src/hooks/use-auth.tsx` exits 0 with **no** `eslint-disable` for `react-hooks/exhaustive-deps` on this file; **or:** exactly one `eslint-disable-next-line react-hooks/exhaustive-deps` immediately preceded by a **multi-line** `/* ... */` comment (min. 3 lines) stating: purpose of mount-only run, why deps are empty (e.g. stable `apiClient`, intentional once-per-mount), reminder to revisit if effect body changes.
    - `grep -n "eslint-disable-next-line react-hooks/exhaustive-deps" src/hooks/use-auth.tsx` — if present, line count is **1** and comment block directly above spans **≥3 lines**.
    - `grep "setIsLoading(false)" src/hooks/use-auth.tsx` — only runs after bootstrap work for the mount completes (not before un-awaited refresh promise starts).
  </acceptance_criteria>
  <action>
    Replace the synchronous `useEffect` body with a **mount-only** pattern: e.g. `useEffect(() => { let cancelled = false; void (async () => { try { ... } finally { if (!cancelled) setIsLoading(false); } })(); return () => { cancelled = true }; }, []);`

    **Branch order (concrete):**
    1. Read `accessToken`, `refreshToken`, `storedUser` raw string from `localStorage.getItem(AUTH_CONFIG.USER_KEY)`.
    2. If `accessToken && storedUser`: parse JSON → `setUser` or clear tokens + remove key on parse error (same as now).
    3. Else if `refreshToken && !accessToken`: `await apiClient.refreshTokens()`. On rejection: `apiClient.clearTokens()`, `localStorage.removeItem(AUTH_CONFIG.USER_KEY)`. On success: re-read `apiClient.getAccessToken()` and if non-null and `storedUser` still present, parse and `setUser` (same parse error handling).
    4. Else: leave `user` null.
    5. In `finally` (when not cancelled), `setIsLoading(false)`.

    Do **not** add `useRouter` to the effect dependency array unless you refactor navigation out of the effect entirely (router stays only in `login`/`logout`).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npx eslint src/hooks/use-auth.tsx</automated>
  </verify>
  <done>Bootstrap is sequential; exhaustive-deps policy documented or satisfied.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npx eslint src/hooks/use-auth.tsx` after Task 1.
- **Trace:** Final AUTH-01 checkbox and `05-VERIFICATION.md` with plan **05-02** if present.
</verification>

<success_criteria>
- No refresh-vs-loading race described in CONCERNS.
- AUTH-01 narrative satisfied in code comments + lint outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-hook-reliability/05-01-SUMMARY.md`
</output>
