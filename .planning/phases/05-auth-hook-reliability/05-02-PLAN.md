---
phase: 05-auth-hook-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/app/(auth)/layout.tsx
  - .planning/phases/05-auth-hook-reliability/05-VALIDATION.md
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Auth layout does not regress redirect/spinner relative to new isLoading semantics; any layout change is minimal and justified in SUMMARY."
    - "05-VALIDATION.md lists automated commands run and manual smoke items for login / refresh / navigation."
  artifacts:
    - path: "src/app/(auth)/layout.tsx"
      provides: "Coherent use of useAuth after provider bootstrap change"
      contains: "useAuth"
    - path: ".planning/phases/05-auth-hook-reliability/05-VALIDATION.md"
      provides: "Phase verification record"
      contains: "npm run build"
  key_links:
    - from: "src/app/(auth)/layout.tsx"
      to: "src/hooks/use-auth.tsx"
      via: "useAuth"
      pattern: "isLoading"
---

<objective>
Validate **AUTH-01** end-to-end after plan **05-01**: ensure **`(auth)/layout.tsx`** still behaves correctly with the new bootstrap timing, add **`05-VALIDATION.md`**, and run **build + test**.

Purpose: No false redirect to `/login` during valid session recovery; planning artifacts record verification.

Output: Possibly unchanged `layout.tsx` (preferred if redundant); new `05-VALIDATION.md`; green `npm run build` and `npm test`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-auth-hook-reliability/05-CONTEXT.md
@.planning/phases/05-auth-hook-reliability/05-01-PLAN.md

<interfaces>
`AuthLayout` (`src/app/(auth)/layout.tsx`): `useEffect` redirects when `!isLoading && !user`; shows `CircularProgress` when `isLoading`; returns `null` when `!user` before redirect.

If 05-01 keeps `isLoading` true until bootstrap completes (including refresh), layout should already be correct — **prefer no layout diff** and document “verified, no change” in SUMMARY.

If a **flicker** or **double redirect** appears in manual smoke, minimal fix allowed: e.g. adjust effect deps or guard **only** with comment referencing race fix — do not weaken auth gating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review AuthLayout against new bootstrap semantics</name>
  <files>src/app/(auth)/layout.tsx</files>
  <read_first>
    - `src/app/(auth)/layout.tsx` — full file
    - `src/hooks/use-auth.tsx` — post-05-01 bootstrap and `isLoading` meaning
  </read_first>
  <acceptance_criteria>
    - **Either:** `git diff src/app/(auth)/layout.tsx` is empty (no layout change) and `05-02-SUMMARY.md` states explicit “no layout change required”; **or:** diff is non-empty and SUMMARY lists one sentence rationale tied to AUTH-01 / bootstrap timing.
    - Layout file still contains `useAuth`, `isLoading`, and redirect to `'/login'` when unauthenticated after load.
    - `grep "eslint-disable" src/app/(auth)/layout.tsx` — no new broad hook disables added.
  </acceptance_criteria>
  <action>
    Compare layout behavior with 05-01 semantics. Change `layout.tsx` **only if** required to avoid incorrect redirect or broken spinner; otherwise leave file unchanged.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Layout aligned or explicitly unchanged with justification.</done>
</task>

<task type="auto">
  <name>Task 2: Phase validation doc and full test run</name>
  <files>.planning/phases/05-auth-hook-reliability/05-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/04-monitor-ux-capture-truthfulness/04-VERIFICATION.md` — structure reference
    - `package.json` — `scripts.build`, `scripts.test`
  </read_first>
  <acceptance_criteria>
    - File `.planning/phases/05-auth-hook-reliability/05-VALIDATION.md` exists with frontmatter `status: draft` or `passed`, `phase: 05-auth-hook-reliability`, `updated:` ISO date.
    - Body includes **Automated checks**: lines stating `npm run build` and `npm test` with **pass** result after execution.
    - Body includes **Human verification** subsection with at least 3 bullet items paraphrasing roadmap success #3: (1) login, (2) navigation between authenticated routes, (3) session with refresh-only cookie path (e.g. simulate or describe clearing access cookie) — **exact manual steps** may say “operator verifies per deploy environment”.
    - `npm test` exits 0 from repo root.
  </acceptance_criteria>
  <action>
    Create `05-VALIDATION.md` mirroring phase 4 verification style (goal, requirement ID **AUTH-01**, evidence pointers to `use-auth.tsx` / layout, automated + human sections). Run `npm run build` and `npm test`; record passes in the file. Set verification verdict to **passed** only if both commands succeeded.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <done>Validation doc written; suite green.</done>
</task>

</tasks>

<verification>
- Repository root: `npm run build`, `npm test`.
- **Trace:** `REQUIREMENTS.md` **AUTH-01** → Done when phase execution completes; `ROADMAP.md` plan checkboxes updated by executor workflow.
</verification>

<success_criteria>
- Documented verification path for phase 5.
- No regressions in build/test.
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-hook-reliability/05-02-SUMMARY.md`
</output>
