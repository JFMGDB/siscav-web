---
phase: 01-planning-contributor-documentation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PLAN-01
must_haves:
  truths:
    - "Os ficheiros PROJECT, REQUIREMENTS, ROADMAP e STATE descrevem brownfield, exclusão de treino de IA e foco formalização + higiene."
    - "A tabela de traceability em REQUIREMENTS mostra 11 requisitos v1 mapeados e 0 unmapped."
    - "Um revisor reproduz as verificações automatizadas em menos de dois minutos."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Visão do produto e Out of Scope (treino de IA)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability e contagens 11/0"
    - path: ".planning/ROADMAP.md"
      provides: "Fases e requisitos por fase"
    - path: ".planning/STATE.md"
      provides: "Estado e foco actual"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "tabela Traceability"
      via: "11 linhas v1 (PLAN-01 … PERF-01)"
---

<objective>
Confirmar que os artefactos GSD cumprem PLAN-01 e o critério D-02 (rastreabilidade v1 sem órfãos, alinhamento com visão do produto).

Purpose: Fechar PLAN-01 com verificação objectiva antes de marcar o requisito como satisfeito.

Output: `REQUIREMENTS.md` com checkbox **PLAN-01** passado para `[x]` após greps/validações passarem.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-planning-contributor-documentation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar PLAN-01 (D-01, D-02) e marcar requisito em REQUIREMENTS</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/01-planning-contributor-documentation/01-CONTEXT.md
  </read_first>
  <action>
    1. Antes de editar: executar mentalmente os checks em `acceptance_criteria` e confirmar leitura rápida de que PROJECT cobre brownfield + `siscav-api` + **Out of Scope** com treino de IA; Active/Validated com formalização + higiene; STATE alinhado à Phase 1 do ROADMAP. Contradições graves entre README e PROJECT tratam-se no plano `01-PLAN-02` ou no SUMMARY — não editar README aqui.
    2. **Pré-voo (antes de editar):** executar um `node -e` que valide apenas traceability (linhas `Mapped to phases: 11`, `Unmapped: 0`, e as 11 linhas `| REQ |` na tabela) — sem exigir ainda `[x] **PLAN-01**` nem `Done`. Saída 0 obrigatória antes de continuar.
    3. Em `.planning/REQUIREMENTS.md`, na secção "Planning & documentation", alterar `- [ ] **PLAN-01**:` para `- [x] **PLAN-01**:` (resto da linha igual). Na tabela Traceability, na linha de `PLAN-01`, trocar `Pending` por `Done` na coluna Status.
    4. Actualizar a linha de rodapé `*Last updated:*` em REQUIREMENTS para a data corrente (formato já usado no ficheiro).
  </action>
  <acceptance_criteria>
    - Ficheiros existem: `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`.
    - `REQUIREMENTS.md` contém a linha `Mapped to phases: 11` e `Unmapped: 0` (secção Traceability).
    - `REQUIREMENTS.md` contém as 11 linhas da tabela de `PLAN-01` até `PERF-01` com colunas Requirement | Phase | Status.
    - `PROJECT.md` contém `treino` (case-insensitive) em secção de âmbito OU menção explícita à exclusão de interface de treino de IA.
    - `ROADMAP.md` na Fase 1 lista requisitos `PLAN-01, PLAN-02`.
    - Após edição: `[x] **PLAN-01**` presente; linha da tabela `PLAN-01` com Status `Done`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\src\personal\siscav-web; node -e "const fs=require('fs');const p='.planning/REQUIREMENTS.md';const t=fs.readFileSync(p,'utf8');if(!t.includes('Mapped to phases: 11')){console.error('missing Mapped to phases: 11');process.exit(1);}if(!t.includes('Unmapped: 0')){console.error('missing Unmapped: 0');process.exit(1);}const rows=['PLAN-01','PLAN-02','API-01','API-02','TYP-01','TYP-02','TYP-03','MON-01','MON-02','AUTH-01','PERF-01'];for(const r of rows){if(!t.includes('| '+r+' ')){console.error('missing trace row',r);process.exit(1);}}if(!t.includes('[x] **PLAN-01**')){console.error('PLAN-01 not checked');process.exit(1);}if(!t.includes('| PLAN-01 | Phase 1 | Done |')){console.error('PLAN-01 trace not Done');process.exit(1);}const pj=fs.readFileSync('.planning/PROJECT.md','utf8');if(!/treino/i.test(pj)){console.error('PROJECT missing treino scope mention');process.exit(1);}const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');if(!rm.includes('PLAN-01, PLAN-02')){console.error('ROADMAP phase1 reqs missing');process.exit(1);}console.log('PLAN-01 verification OK');"</automated>
  </verify>
  <done>Traceability e conteúdo verificados; PLAN-01 marcado `[x]` e tabela actualizada para `Done`.</done>
</task>

</tasks>

<verification>
Re-executar o `node -e` da Task 1 (deve terminar com exit 0 após as edições).
</verification>

<success_criteria>
PLAN-01 verificado por automação; REQUIREMENTS mostra PLAN-01 cumprido na lista e na tabela de traceability.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-planning-contributor-documentation/01-planning-contributor-documentation-1-SUMMARY.md` (ou nome equivalente exigido pelo execute-plan do GSD).
</output>
