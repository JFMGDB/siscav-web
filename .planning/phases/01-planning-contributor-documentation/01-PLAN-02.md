---
phase: 01-planning-contributor-documentation
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - .gitignore
  - .env.example
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PLAN-02
must_haves:
  truths:
    - "`.env.example` na raiz documenta `NEXT_PUBLIC_API_URL` com exemplo `http://localhost:8000` e texto em português (termos técnicos em inglês)."
    - "README explica como copiar para `.env.local` e apontar ao backend `siscav-api`."
    - "`.env.example` não está ignorado pelo git (padrão `!.env.example` após `.env*`)."
    - "README menciona brevemente `.planning/` para contexto GSD (D-06)."
  artifacts:
    - path: ".env.example"
      provides: "Variável NEXT_PUBLIC_API_URL documentada sem segredos"
    - path: "README.md"
      provides: "Secção Configuração + menção Planeamento"
    - path: ".gitignore"
      provides: "Excepção !.env.example"
  key_links:
    - from: "README.md"
      to: "NEXT_PUBLIC_API_URL"
      via: "secção Configuração local / API"
    - from: ".env.example"
      to: "src/constants/index.ts"
      via: "mesmo default http://localhost:8000 quando env ausente"
---

<objective>
Documentar `NEXT_PUBLIC_API_URL` para onboarding (D-03, D-04, D-05), garantir que `.env.example` é versionado (D-06), e referenciar `.planning/` no README (D-06).

Purpose: Cumprir PLAN-02 e o critério 3 do ROADMAP para novos contribuidores.

Output: `.gitignore` com `!.env.example`, ficheiro `.env.example` na raiz, `README.md` actualizado em português, PLAN-02 marcado em `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planning-contributor-documentation/01-CONTEXT.md
@src/constants/index.ts
@README.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Versionar `.env.example` e ignorar apenas `.env` local (D-03, D-04, D-06)</name>
  <files>.gitignore,.env.example</files>
  <read_first>
    - .gitignore
    - src/constants/index.ts
    - .planning/phases/01-planning-contributor-documentation/01-CONTEXT.md
  </read_first>
  <action>
    1. Em `.gitignore`, imediatamente após o bloco que contém a linha `.env*` (comentário "env files"), adicionar uma linha **`!.env.example`** para que o ficheiro de exemplo seja rastreado pelo git — per D-06 e porque `.env*` actual ignora também `.env.example`.
    2. Criar na **raiz** do repositório o ficheiro **`.env.example`** com:
       - Uma ou duas linhas de comentário em **português** explicando que esta variável define a URL base da API do backend **`siscav-api`**, que em desenvolvimento local costuma ser **`http://localhost:8000`**, alinhado ao fallback em `API_CONFIG.BASE_URL` em `src/constants/index.ts` quando `NEXT_PUBLIC_API_URL` não está definida.
       - Uma linha **exactamente** no formato: `NEXT_PUBLIC_API_URL=http://localhost:8000` (sem aspas; valor de exemplo conforme CONTEXT).
       - Manter nomes técnicos `NEXT_PUBLIC_API_URL` e URLs em inglês/notação técnica; prosa em português (D-05).
    3. Não incluir segredos, tokens ou `.env.local` no repositório.
  </action>
  <acceptance_criteria>
    - `git check-ignore -v .env.example` **não** lista `.env.example` como ignorado (comando a correr na raiz; se `git check-ignore` retorna vazio ou não inclui `.env.example`, OK).
    - Ficheiro `.env.example` existe e contém substring `NEXT_PUBLIC_API_URL=http://localhost:8000`.
    - `.gitignore` contém linha `!.env.example` após a regra `.env*`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\src\personal\siscav-web; node -e "const fs=require('fs');const g=fs.readFileSync('.gitignore','utf8');if(!g.includes('.env*')){console.error('.env* missing');process.exit(1);}if(!g.includes('!.env.example')){console.error('!.env.example missing');process.exit(1);}const e=fs.readFileSync('.env.example','utf8');if(!e.includes('NEXT_PUBLIC_API_URL=http://localhost:8000')){console.error('env example line missing');process.exit(1);}console.log('.env.example + gitignore OK');"</automated>
  </verify>
  <done>`.env.example` criado com variável e texto PT; `.gitignore` permite tracking do exemplo.</done>
</task>

<task type="auto">
  <name>Task 2: README — Configuração da API e menção a `.planning/` (D-03, D-05, D-06)</name>
  <files>README.md,.planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md
    - .env.example
    - .planning/PROJECT.md
  </read_first>
  <action>
    1. No **README.md** (português), após **Guia de Instalação** / **Instalação** ou dentro de **Pré-requisitos**, adicionar subsecção **`### Configuração local da API`** (ou título equivalente curto) que:
       - Explique que o frontend fala com o backend no repositório **`siscav-api`** via REST.
       - Documente a variável de ambiente **`NEXT_PUBLIC_API_URL`** (nome exacto) como URL base da API.
       - Indique que o valor típico em desenvolvimento é **`http://localhost:8000`**, coerente com o fallback em `src/constants/index.ts` quando a variável não está definida.
       - Instrua a copiar `.env.example` para **`.env.local`** (`cp` em Unix ou `Copy-Item` em PowerShell — pode mostrar ambos num bloco de código ou escolher uma linha + nota "no Windows, use Copy-Item").
    2. Na secção **Estrutura do Projeto** (ou subsecção **`### Planeamento`** imediatamente após o bloco `bash` da árvore), adicionar 1–3 linhas em português: a pasta **`.planning/`** contém artefactos GSD (ex.: roadmap, requisitos, estado) para quem mantém o projeto; não é obrigatório para uma alteração pontual, mas é a fonte de escopo acordado (D-06).
    3. Confirmar que a **Visão Geral** do README não contradiz a secção **What This Is** de `.planning/PROJECT.md` (painel admin, dois repositórios, sem prometer treino de IA). Ajustar apenas se houver contradição factual.
    4. Em `.planning/REQUIREMENTS.md`, após esta documentação estar completa: marcar **PLAN-02** checkbox `[x]` e actualizar a linha de traceability de `PLAN-02` para o mesmo estado usado para PLAN-01 na conclusão da fase (`Done`).
  </action>
  <acceptance_criteria>
    - README contém `NEXT_PUBLIC_API_URL` e `http://localhost:8000` e referência a `.env.example` e `.env.local`.
    - README contém menção a `.planning/` e a roadmap/requisitos ou GSD (pelo menos uma frase).
    - `Select-String -Path 'README.md' -Pattern 'NEXT_PUBLIC_API_URL'` devolve match.
    - `REQUIREMENTS.md` contém `[x] **PLAN-02**`.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\src\personal\siscav-web; node -e "const fs=require('fs');const r=fs.readFileSync('README.md','utf8');if(!r.includes('NEXT_PUBLIC_API_URL'))process.exit(1);if(!r.includes('http://localhost:8000'))process.exit(1);if(!r.includes('.env.example'))process.exit(1);if(!r.includes('.planning'))process.exit(1);const q=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');if(!q.includes('[x] **PLAN-02**'))process.exit(1);console.log('README + PLAN-02 OK');"</automated>
  </verify>
  <done>README actualizado em PT; PLAN-02 satisfeito e marcado em REQUIREMENTS.</done>
</task>

</tasks>

<verification>
`git check-ignore -v .env.example` não ignora o ficheiro; `npm run build` na raiz conclui sem erro (valida que documentação não quebrou o projecto).
</verification>

<success_criteria>
Novos contribuidores conseguem configurar `NEXT_PUBLIC_API_URL` via README + `.env.example`; exemplo de env versionado; PLAN-02 `[x]` em REQUIREMENTS.
</success_criteria>

<output>
Após conclusão, criar ficheiro SUMMARY da fase para o plano 2 conforme workflow execute-plan.
</output>
