# Summary — Plan 07-01

- Implementada rota `/camera`, componentes `UsbCameraPreview`, `NetworkCameraPreview`, `CameraPreviewPanel`.
- `validate-camera-url` + testes; dependência `hls.js` com import dinâmico.
- `ROUTES.AUTH.CAMERA` e entrada no `Sidebar`.
