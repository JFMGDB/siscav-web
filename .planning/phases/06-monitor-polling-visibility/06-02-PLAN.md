---
phase: 06-monitor-polling-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .planning/phases/06-monitor-polling-visibility/06-VALIDATION.md
  - .planning/phases/06-monitor-polling-visibility/06-VERIFICATION.md
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "06-VALIDATION.md documents automated passes and human DevTools steps for background vs foreground polling."
    - "06-VERIFICATION.md status passed when build and test succeed."
  artifacts:
    - path: ".planning/phases/06-monitor-polling-visibility/06-VALIDATION.md"
      provides: "Validation record for phase 6"
      contains: "npm run build"
    - path: ".planning/phases/06-monitor-polling-visibility/06-VERIFICATION.md"
      provides: "Phase verification verdict"
      contains: "PERF-01"
  key_links:
    - from: ".planning/phases/06-monitor-polling-visibility/06-VALIDATION.md"
      to: "src/hooks/use-monitor-capture.ts"
      via: "evidence"
      pattern: "use-monitor-capture"
---

<objective>
Record **PERF-01** verification: **`06-VALIDATION.md`** and **`06-VERIFICATION.md`**, run full **`npm test`**, ensure **no regressions** after plan **06-01**.

Purpose: Traceability and operator checklist (DevTools network while tab backgrounded).

Output: Two markdown files under the phase directory; green test suite.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-monitor-polling-visibility/06-CONTEXT.md
@.planning/phases/06-monitor-polling-visibility/06-01-PLAN.md
@.planning/phases/05-auth-hook-reliability/05-VALIDATION.md

<interfaces>
Mirror **05-VALIDATION** / **04-VERIFICATION** structure: frontmatter, goal, requirement table, automated, human (DevTools), verdict **passed** only if `npm run build` and `npm test` exit 0 after plan 06-01 is merged in branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validation and verification markdown</name>
  <files>.planning/phases/06-monitor-polling-visibility/06-VALIDATION.md
.planning/phases/06-monitor-polling-visibility/06-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/05-auth-hook-reliability/05-VALIDATION.md`
    - `.planning/phases/04-monitor-ux-capture-truthfulness/04-VERIFICATION.md`
  </read_first>
  <acceptance_criteria>
    - Both files exist under `.planning/phases/06-monitor-polling-visibility/` with YAML frontmatter: `phase: 06-monitor-polling-visibility`, `updated:` ISO date, `status: passed` (or draft then updated to passed after tests).
    - **06-VALIDATION.md** body lists **`npm run build`** and **`npm test`** with **pass** after execution.
    - **Human verification** subsection includes **≥3** bullets: (1) open monitor route, (2) confirm periodic requests while tab **visible**, (3) switch tab away / minimize so `visibilityState` is **hidden** and confirm **no** continued same-interval polling (or clear cessation), (4) return visible and confirm **resume** without full page reload.
    - **06-VERIFICATION.md** references **PERF-01** and points to `use-monitor-capture.ts`.
  </acceptance_criteria>
  <action>
    Create both files. Write human steps in **Portuguese** to match the rest of `.planning`. Set verdict **passed** only after Task 2 succeeds.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Docs drafted; finalize status after tests.</done>
</task>

<task type="auto">
  <name>Task 2: Full automated test run</name>
  <files>package.json</files>
  <read_first>
    - `package.json` — `scripts.test`
  </read_first>
  <acceptance_criteria>
    - `npm test` from repository root exits **0**.
    - If Task 1 left `status: draft`, update both validation/verification frontmatter to **`passed`** after this task.
  </acceptance_criteria>
  <action>
    Run `npm test`. Update frontmatter in **06-VALIDATION.md** and **06-VERIFICATION.md** to `status: passed` if build and test are green.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Suite green; verification locked.</done>
</task>

</tasks>

<verification>
- `npm run build` and `npm test` from repository root.
- **Trace:** `REQUIREMENTS.md` **PERF-01** after phase execution; `ROADMAP.md` plan checkboxes.
</verification>

<success_criteria>
- Documented manual DevTools path for PERF-01.
- Automated checks green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-monitor-polling-visibility/06-02-SUMMARY.md`
</output>
